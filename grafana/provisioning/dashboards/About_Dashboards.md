🚀 Result
With this dashboard + the unified one I gave earlier:

You’ll see system-wide observability (metrics, logs, traces).

And a deep dive into SSE/WebFlux performance for your reactive notification system.

---------------------------

🔧 What This Dashboard Shows
Active SSE Connections → how many clients are currently connected.

Throughput (Events/sec) → rate of events pushed per second.

Latency (p95) → 95th percentile latency for /trace-file/** endpoints.

SSE Logs → filtered logs from Loki containing “SSE”.

Reactive Stream Traces → Tempo traces for debugging stream bottlenecks.

-------------------------

